package main

import "fmt"

func main() {
	// go is a statically typed lang, that means varibales can only have a single type
	username := "sachin"
	password := "12345"

	fmt.Println("The basic auth is : ", username+"@"+password)
}
